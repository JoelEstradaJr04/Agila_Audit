# ============================================================================
# RAILWAY DEPLOYMENT - Backend + Database Environment Variables
# ============================================================================
# Copy these variables to your Railway project settings
# Dashboard > Project > Variables
# ============================================================================

# ────────────────────────────────
# 🔧 Server Configuration
# ────────────────────────────────
NODE_ENV=production

# Railway provides PORT automatically - don't override unless needed
# PORT=${{RAILWAY_PORT}}

# Backend URL (auto-generated by Railway)
# Example: https://your-backend.railway.app
BACKEND_URL=${{RAILWAY_PUBLIC_DOMAIN}}

# ────────────────────────────────
# 🔗 CORS Configuration
# ────────────────────────────────
# Add your Vercel frontend URLs (comma-separated)
# Example: https://your-app.vercel.app,https://your-app-preview.vercel.app
CORS_ORIGIN=https://your-frontend.vercel.app,https://your-frontend-*.vercel.app

# ────────────────────────────────
# 🗄️ PostgreSQL Database (Railway)
# ────────────────────────────────
# Railway auto-provides these when you add PostgreSQL service:
# DATABASE_URL=${{Postgres.DATABASE_URL}}
# 
# Map it to your Prisma variable:
AUDIT_LOGS_DATABASE_URL=${{Postgres.DATABASE_URL}}

# Shadow database for Prisma migrations (optional but recommended)
# Create a second database in Railway or use a suffix approach
SHADOW_DATABASE_URL=${{Postgres.DATABASE_URL}}_shadow

# ────────────────────────────────
# 🔐 Security and Auth Config
# ────────────────────────────────
# CRITICAL: Use strong, unique secrets in production
JWT_SECRET=your-production-jwt-secret-minimum-32-characters-CHANGE-THIS

# Service API Keys (generate strong keys for production)
FINANCE_API_KEY=FINANCE_PRODUCTION_KEY_CHANGE_THIS
HR_API_KEY=HR_PRODUCTION_KEY_CHANGE_THIS
INVENTORY_API_KEY=INVENTORY_PRODUCTION_KEY_CHANGE_THIS
OPERATIONS_API_KEY=OPERATIONS_PRODUCTION_KEY_CHANGE_THIS
BUDGET_API_KEY=BUDGET_PRODUCTION_KEY_CHANGE_THIS

# ────────────────────────────────
# 📊 Service Configuration
# ────────────────────────────────
SERVICE_NAME=audit-logs-microservice
PRISMA_LOG_LEVEL=warn

# ────────────────────────────────
# 🚫 NEVER USE IN PRODUCTION
# ────────────────────────────────
# DISABLE_AUTH=false  # Keep commented out - NEVER enable in production!

# ────────────────────────────────
# ⚠️ RAILWAY SETUP STEPS
# ────────────────────────────────
# 1. Create Railway project from your GitHub repo
# 2. Add PostgreSQL database service to project
# 3. Railway will auto-generate DATABASE_URL
# 4. Add all variables above to Railway project
# 5. Use ${{Postgres.DATABASE_URL}} to reference Railway's PostgreSQL
# 6. Generate domain: Settings > Networking > Generate Domain
# 7. Update CORS_ORIGIN with your Vercel frontend URL
# 8. Deploy will auto-trigger on push to main/master branch
