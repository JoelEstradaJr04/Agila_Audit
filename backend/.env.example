# ============================================================================
# AUDIT LOGS MICROSERVICE - Environment Configuration
# ============================================================================

# ────────────────────────────────
# 🔧 Server Configuration
# ────────────────────────────────
NODE_ENV=development

# Frontend (Next.js) Configuration
PORT=4010
HOST=localhost
BASE_URL=http://localhost:4010

# Backend (Express API) Configuration
BACKEND_PORT=4011
BACKEND_URL=http://localhost:4011
NEXT_PUBLIC_API_BASE_URL=http://localhost:4011

# CORS - Allow frontend to access backend API
CORS_ORIGIN=http://localhost:4010

# ────────────────────────────────
# 🗄️ Database Connection
# ────────────────────────────────
AUDIT_LOGS_DATABASE_URL="postgresql://postgres:nerieannganda247@localhost:5432/audit_final_db?schema=public"
SHADOW_DATABASE_URL="postgresql://postgres:nerieannganda247@localhost:5432/audit_final_shadow_db?schema=public"

# ────────────────────────────────
# 🔐 Security and Auth Config
# ────────────────────────────────
# JWT Secret (must match FTMS/Budget for token verification)
JWT_SECRET=JWT_SECRTET_HERE

# Service API Keys (for machine-to-machine authentication)
# These are default keys - change in production and store securely
FINANCE_API_KEY=FINANCE_DEFAULT_KEY
HR_API_KEY=HR_DEFAULT_KEY
INVENTORY_API_KEY=INVENTORY_DEFAULT_KEY
OPERATIONS_API_KEY=OPERATIONS_DEFAULT_KEY
BUDGET_API_KEY=BUDGET_DEFAULT_KEY

# Alternative: Use JSON array for dynamic service keys
# SERVICE_API_KEYS_JSON='[{"name":"finance","key":"FINANCE_KEY","canWrite":true},{"name":"budget","key":"BUDGET_KEY","canWrite":true}]'

# ────────────────────────────────
#  Service Configuration
# ────────────────────────────────
SERVICE_NAME=audit-logs-microservice
PRISMA_LOG_LEVEL=info

# ────────────────────────────────
# 🔓 TESTING MODE (Disable Authentication)
# ────────────────────────────────
# Set to 'true' ONLY for local testing - NEVER in production
# ⚠️ WARNING: This bypasses all authentication!
DISABLE_AUTH=true

# Mock user credentials when DISABLE_AUTH=true
TEST_USER_ID=test_user_001
TEST_USERNAME=test_admin
TEST_USER_ROLE=SuperAdmin
# Role Options: SuperAdmin, Finance Admin, Finance Non-Admin, HR Admin, etc.

# Mock service name when DISABLE_AUTH=true
TEST_SERVICE_NAME=finance
# Options: finance, hr, inventory, operations, budget
